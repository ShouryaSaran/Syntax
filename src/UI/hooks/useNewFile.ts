import type { FileNode } from "../components/data/explorerData";

const useNewFile = () => {
    const handleNewFile = (item:FileNode) =>{

    };

    return handleNewFile;
}

export default useNewFile;